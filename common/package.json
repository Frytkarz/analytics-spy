{
    "name": "common",
    "version": "0.0.1",
    "description": "This is module with common stuff for functions and hosting. It does not have to be build or added to package.json as long as you treat src folder as simple subdirectory.",
    "main": "index.js",
    "scripts": {
        "lint": "tslint --project tsconfig.json",
        "prebuild": "npm run lint",
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Maciej Frydrychowicz",
    "license": "N/A",
    "devDependencies": {
        "tslint": "^5.11.0",
        "typescript": "^3.0.3"
    }
}
